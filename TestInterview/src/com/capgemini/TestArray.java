package com.capgemini;

import java.util.Arrays;

public class TestArray {
	public static void process(int n, int[] ar) {
		// int[] array = {0, 0, 0};
		// IntStream.range(0, 1000).map(n -> (array[]));
		// System.out.println("intial ar " + ar);
		int index = ar.length - 1;
		System.out.println("initial index " + index);
		for (int num : ar) {

		} // end of for loop

		// for (int item : ar) {
		// System.out.println("item inside loop " + item);
		//
		// item = Integer
		// .parseInt(String.valueOf(n)
		// .substring(index > 0 ? index - 1 : 0, index)) > 0
		// && !String.valueOf(n)
		// .substring(index > 0 ? index - 1 : 0, index)
		// .isEmpty()
		// ? Integer.parseInt(
		// String.valueOf(n).substring(
		// index > 0 ? index - 1 : 0,
		// index))
		// : 0;
		// System.out.println("item " + item);
		// ar[index] = item;
		//
		// index = index < 0 ? 0 : --index;
		// System.out.println("index inside loop " + index);
		// System.out.println(" array after iteration " + Arrays.toString(ar));
		// }

	}

	public static void main(String[] s) {
		// process();
		// System.out.println("final ar " + Arrays.deepToString(ar));
		// Arrays.deepToString(ar);
		// Arrays.f
		int n = 999;// {9,9,9}
		int[] ar = {0, 0, 0};
		process(n, ar);
		// Map<Integer, > map = new HashMap<>();
		Integer integer = 999;
		// map.add(integer,);
		System.out.println("int list " + Arrays.toString(ar));
		// map.add(integer, Arrays.asList(intAr));

	}
}
